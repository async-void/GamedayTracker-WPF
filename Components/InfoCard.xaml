<UserControl x:Class="GamedayTracker.Components.InfoCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GamedayTracker.Components"
             mc:Ignorable="d" 
             Height="250" Width="400">
    <Grid>
         <Grid.ColumnDefinitions>
             <ColumnDefinition Width="20" />
             <ColumnDefinition Width="*" />
             <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <!-- #region Header -->
        <Border Grid.Column="0"
                Grid.Row="0"
                Grid.ColumnSpan="3"
                CornerRadius="6 6 0 0"
                Background="{DynamicResource PrimaryToSecondaryGradientBrush}"
                BorderBrush="WhiteSmoke"
                BorderThickness="0 0 0 .5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="Player Info"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource Primary900ColorBrush}"
                           FontSize="18"
                           FontWeight="Bold"/>
            </Grid>
        </Border>
        <!-- #endregion -->

    </Grid>
</UserControl>
